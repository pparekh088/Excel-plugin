/* global Office */

Office.onReady(() => {
  // No ExecuteFunction commands in Phase 0; the ribbon button opens the task
  // pane directly. This runtime exists so the manifest's FunctionFile resolves.
});
